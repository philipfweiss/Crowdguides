<% provide(:title, 'All users') %>
<h1 class="center">All Guides</h1>

<%= will_paginate %>

<ul class="users">
    <% @guides.each do |guide| %>
        <div>
            <h3>
                <%= link_to guide.title, guide  %>
            </h3>
            <div>
                <%= guide.description %>
            </div>

            <!--<div>-->
                <!--<%= guide.content %>-->
            <!--</div>-->
        </div>
    <% end %>
</ul>

<%= will_paginate %>